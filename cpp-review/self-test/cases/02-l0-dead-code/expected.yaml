must_fire:
  - id: E1
    tier: SHOULD
    signal: "L0 dead code: ContextCache has zero production callers"
    keywords: ["ContextCache", "dead", "caller", "production", "test", "ContextCacheTests", "no.*caller", "zero"]
    match: any_2

must_not_fire: []

thresholds:
  min_recall: 1.0
  max_noise_ratio: 0.80
